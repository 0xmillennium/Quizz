spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/online_quiz
    username: online_quiz
    password: online_quiz
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
