/**
 * Production-jar command-line tooling.
 *
 * <p>
 * Tooling commands are intentionally narrow and suitable for local or
 * deployment-time operations. The password hashing mode reads from standard
 * input, writes only the hash to standard output, and reports validation errors
 * to standard error.
 * </p>
 */
package com.quizz.tooling;
