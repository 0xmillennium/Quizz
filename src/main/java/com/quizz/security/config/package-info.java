/**
 * HTTP security configuration.
 *
 * <p>
 * This subpackage declares route authorization, form-login, logout, CSRF,
 * and password-encoder wiring. It should remain configuration-only and should
 * not contain domain authorization decisions beyond web route grouping.
 * </p>
 */
package com.quizz.security.config;
